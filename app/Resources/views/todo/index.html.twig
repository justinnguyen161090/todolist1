{% extends 'base.html.twig' %}
  {% block body %} 
        <h2 class="text-center">Registration Form</h2>
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
    {% endblock %} 